# SEDF - SSRF Exploitation and Defense Framework
# Python dependencies

# Core HTTP client
requests>=2.31.0
urllib3>=2.0.0

# Optional: faster async HTTP (used if available)
httpx>=0.25.0

# Lab application (only needed for docker/vulnerable_app)
flask>=3.0.0

# Development & testing
pytest>=7.4.0
pytest-cov>=4.1.0
